import {wooBridge,commerceConnected} from '@/lib/commerce';
import {json,sameOrigin,smallBody} from '@/lib/researcher';
export async function POST(request:Request){if(!sameOrigin(request))return json({error:'Request origin is not allowed.'},403);try{const b=await smallBody(request);if(b.website||typeof b.name!=='string'||b.name.length<2||b.name.length>150||typeof b.email!=='string'||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(b.email)||typeof b.message!=='string'||b.message.length<10||b.message.length>5000)throw new Error('Please include your name, email, and a message of at least 10 characters.');if(!commerceConnected())return json({error:'The contact form is not connected yet. Email support.lab@innoaminos.com or call 630-901-7000.'},503);const result=await wooBridge('contact',{name:b.name,email:b.email,message:b.message,phone:String(b.phone||'').slice(0,40),subject:String(b.subject||'General question').slice(0,120)});if(!result.sent)throw new Error('Message delivery could not be confirmed. Please email our team directly.');return json({sent:true})}catch(e){return json({error:e instanceof Error?e.message:'Unable to send your message.'},400)}}
